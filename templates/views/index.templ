package views

import (
	"github.com/ReidMason/plex-ani-sync/templates/layouts"
	"github.com/ReidMason/plex-ani-sync/internal/mediaHost"
)

templ Index(mediaHostUser mediaHost.User) {
	@layouts.Main() {
		<div class="bg-slate-700 p-8 h-full text-slate-100">
			<h1 class="font-semibold text-xl">Home</h1>
			<p>Welcome, { mediaHostUser.Username }</p>
		</div>
	}
}
