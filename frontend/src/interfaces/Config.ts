export default interface Config {
    ANIME_LIBRARIES: Array<string>;
    MARK_UNWATCHED_EPISODES_AS_PLANNING: boolean;
    SYNC_CRONTIME: string;
    SYNC_SCHEDULE_ENABLED: boolean;
    DATE_FORMAT: string;
}